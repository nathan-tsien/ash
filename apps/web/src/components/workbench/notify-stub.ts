/** Phase 1 placeholder for artifact preview feedback; replace with toast in Phase 2. */
export function notifyStub(message: string): void {
  console.info("[ash] artifact preview stub:", message);
}
